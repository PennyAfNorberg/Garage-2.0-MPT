@model Garage_2._0_MPT.Models.ParkingsHouseStatusViewModel
<div class="row Gobig" >
    <table class="align-self-center border-dark wider" >

        <thead>
            <tr><th colspan="4" class="text-center">Next available places</th></tr>
            <tr>
                @foreach (var item in Model.NextFree)
                {
                    <th>@item.Key</th>
                }
            </tr>
        </thead>
        <tbody>
            <tr>
                @foreach (var item in Model.NextFree)
                {
                    <td>@item.Value</td>
                }

            </tr>

        </tbody>


    </table>

</div>


